// 获取token
export const fetchToken = async function() {
    // const res = await this.$axios.get('/wcm/mlfcenter.do', {
    //     params: {
    //         serviceid: 'getToken',
    //         tenantId: this.$store.state.session.mem.TENANTID
    //     }
    // });
    // if (res) {
    //     this.$localstore.set('token', res.token, 60 * 60 + 50 * 60); // token 2小时过期 需要提前清空
    //     document.cookie = `ls.userToken=${res.token}`;
    // }
};
